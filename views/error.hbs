{{! extends layout - no Handlebars padrão, isso depende do engine usado (como express-handlebars) }}
{{!-- Se estiver usando express-handlebars, layouts são configurados assim: --}}

{{!-- No arquivo de layout principal (ex: layout.hbs), você teria algo como: --}}
<!DOCTYPE html>
<html>
<head>
  <title>Erro</title>
</head>
<body>
  {{{ body }}}
</body>
</html>

{{!-- E agora o seu template de erro ficaria assim: --}}

<h1>{{message}}</h1>
<h2>{{error.status}}</h2>
<pre>{{error.stack}}</pre>
