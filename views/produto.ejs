<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

    <title>
        <%= titulo %>
    </title>

    <style>
        body {
            background-color: #f8f9fa;
        }

        .card-title strong {
            color: #0d6efd;
        }

        .card-img-top {
            width: 50%;
            /* Define a largura da imagem para 50% */
            height: auto;
            /* Garante que a altura seja proporcional à largura */
            object-fit: cover;
            display: block;
            margin-left: auto;
            margin-right: auto;
            /* Centraliza a imagem */
        }
    </style>
</head>

<body>
    <div class="container mt-4">
        <h1 class="display-5 mb-4">
            <%= titulo %>
        </h1>

        <% if (produtosParaView && produtosParaView.length> 0) { %>
            <div class="row">
                <% produtosParaView.forEach(produtos=> { %>
                    <div class="col-md-6 col-lg-4 mb-3">
                        <div class="card h-100">
                            <img src="<%= produtos.imagem %>" class="card-img-top" alt="<%= produtos.nome %>">
                            <!-- Imagem do produto -->
                            <div class="card-body d-flex flex-column">
                                <h5 class="card-title">
                                    <strong>
                                        <%= produtos.nome %>
                                    </strong>
                                    <small class="text-muted fw-normal">(ID: <%= produtos.id %>)</small>
                                </h5>
                                <p class="card-text flex-grow-1">
                                    <%= produtos.descricao %>
                                </p>
                                <a href="#" class="btn btn-sm btn-outline-primary mt-auto">Ver Detalhes</a>
                            </div>
                        </div>
                    </div>
                    <% }); %>
            </div>
            <% } else { %>
                <div class="alert alert-warning" role="alert">
                    Nenhum produto para exibir no momento.
                </div>
                <% } %>

                    <hr class="my-4">
                    <a href="/" class="btn btn-secondary">Voltar para a página inicial</a>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
</body>

</html>