<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <!-- Viewport essencial para responsividade -->
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- Bootstrap 5 CSS (via CDN) -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

    <!-- Usando a variável 'titulo' passada pelo Express -->
    <title>
        <%= titulo %>
    </title>

    <style>
        /* Estilos personalizados */
        body {
            background-color: #f8f9fa;
        }

        .card-title strong {
            color: #0d6efd;
        }

        .card-img-top {
            object-fit: cover;
            height: 200px;
            width: 100%;
        }
    </style>
</head>

<body>
    <div class="container mt-4">
        <h1 class="display-5 mb-4">
            <%= titulo %>
        </h1>

        <!-- Verificando se o array 'itensParaView' existe e tem itens -->
        <% if (itensParaView && itensParaView.length> 0) { %>
            <div class="row">
                <!-- Iterando sobre o array 'itensParaView' -->
                <% itensParaView.forEach(item=> { %>
                    <div class="col-md-6 col-lg-4 mb-3">
                        <!-- Card do produto -->
                        <div class="card h-100">
                            <!-- Imagem do produto -->
                            <img src=<%= item.imagem %> class="card-img-top" alt="<%= item.nome %>">
                            <!-- A URL da imagem vem da propriedade 'imagem' de cada item -->

                            <div class="card-body d-flex flex-column">
                                <!-- Corpo do card -->
                                <h5 class="card-title">
                                    <strong>
                                        <%= item.nome %>
                                    </strong>
                                    <small class="text-muted fw-normal"> (ID: <%= item.id %>)</small>
                                </h5>
                                <p class="card-text flex-grow-1">
                                    <%= item.descricao %>
                                </p>
                                <a href="#" class="btn btn-sm btn-outline-primary mt-auto">Ver Detalhes</a>
                            </div>
                        </div>
                    </div>
                    <% }); %>
            </div>
            <% } else { %>
                <div class="alert alert-warning" role="alert">
                    Nenhum item para exibir no momento.
                </div>
                <% } %>

                    <hr class="my-4">
                    <a href="/" class="btn btn-secondary">Voltar para a página inicial</a>
    </div>

    <!-- Bootstrap 5 Bundle JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
</body>

</html>